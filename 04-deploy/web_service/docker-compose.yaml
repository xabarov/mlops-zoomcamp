services:
  nyc-taxi-predictor:
    build:
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - MLFLOW_TRACKING_URI=http://localhost:5000
      - EXPERIMENT_NAME=nyc-taxi
      - RUN_ID=4bb404017aa548e992a15a6c65276aa0 # Replace with your actual RUN_ID


  # mlflow-server:
  #   image: ghcr.io/mlflow/mlflow:v3.1.1
  #   ports:
  #     - "5000:5000"
  #   volumes:
  #     - ./mlruns:/mlruns
  #   command: "mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri sqlite:///my.db"
